# editorial-hub

Editorial Hub — статический сайт редакционной политики для мультиплатформенных контент-проектов на русском и казахском языках, с разделом об использовании ИИ, визуально выровненный по AV DS 4 без привязки к другой платформе и без внешней зависимости от шрифтов.

## Что внутри

- Спокойный документный интерфейс в духе AV DS 4 со светлой темой по умолчанию
- Группировка разделов, поиск по правилам и быстрый экспорт в Markdown
- Статические данные без сборки и без серверных зависимостей

## Локальный запуск

Откройте `index.html` напрямую в браузере или поднимите любой статический сервер, например:

```bash
python3 -m http.server 8080
```

## Структура

- `index.html` — оболочка интерфейса и точки входа
- `styles.css` — визуальный слой
- `app.js` — рендеринг, навигация, темы, экспорт
- `data.js` — контент разделов и метаданные
- `CONTENT_UPGRADE.md` — карта содержательной доработки и последующей чистки оболочки
